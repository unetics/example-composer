name: CI-Composer-v4

on: [push]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        uses: php-actions/composer@v4
        with:
          composer_version: 1
          php_version: 8.0
